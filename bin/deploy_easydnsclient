#!/bin/sh

# READY

######################################################################
# Script version is yymmdd-HHMMSS in UTC, date -u +%y%m%d-%H%M%S
######################################################################
SCRIPTVERSION=150626-151355

pkg install -y p5-Crypt-SSLeay p5-DNS-EasyDNS

mkdir -p ~/bin
cat >~/bin/easydns-client <<EOF
#!/bin/sh
perl /usr/local/share/examples/DNS-EasyDNS/easydns-simple \$*
EOF
chmod 755 ~/bin/easydns-client

echo
echo "Usage as user urep: easydns-client username password hostname"
echo "crontabbed as user urep: 0 * * * * easydns-client username password hostname"
