#!/bin/sh

# READY

if sysctl security.jail.jailed | grep 1 >/dev/null 2>&1; then
    echo "ERROR: Cannot run in a jail"
    exit 1
fi

if [ "x" = "x${FQDN}" ]; then
    echo "Please set or export variable FQDN"
    exit 1
fi

if [ "x" = "x${REPOSRC}" ]; then
    REPOSRC=https://github.com/johnko
fi

if [ "x" = "x${GITLABSRC}" ]; then
    GITLABSRC=https://gitlab.com/
fi

if [ "x" = "x${NIC}" ]; then
    NIC="lo1"
    if ! ifconfig ${NIC} >/dev/null ; then
        ifconfig ${NIC} create
    fi
fi

if [ "x" = "x${IPNET}" ]; then
    IPNET="10.7.7."
fi

if [ "x" = "x${HOSTIP}" ]; then
    HOSTIP="$( net-ip )"
fi

if [ "x" = "x${HOSTNIC}" ]; then
    HOSTNIC="$( net-nic )"
fi

if ! iocage list -r | grep 'RELEASE' >/dev/null 2>/dev/null ; then
    ioc-setup
    if ! iocage list -r | grep 'RELEASE' >/dev/null 2>/dev/null ; then
        echo "ERROR: iocage release doesn't exist, something went wrong."
        exit 1
    fi
fi

if ! iocage list -t | grep 'common' >/dev/null 2>/dev/null ; then
    ioc-newtemplate common 'lo1|10.7.7.253/32'
    if ! iocage list -t | grep 'common' >/dev/null 2>/dev/null ; then
        echo "ERROR: iocage common template doesn't exist, something went wrong."
        exit 1
    fi
fi

if [ "x" != "x${STARTCOUNT}" ]; then
    # remove anything that's not a digit
    count=`echo ${STARTCOUNT} | sed 's/[^[:digit:]]//g'`
fi

if [ "x" = "x${count}" ]; then
    count=0
fi



jailip() {
    local name
    local num
    JAILDATASET="${JAILSGOOD} ${JAILSDEV} ${JAILSBAD}"
    echo "$JAILDATASET" \
        | while read num name ; do
            if [ "x" != "x${num}" ] && [ "x" != "x${name}" ]; then
                #echo "debug: search ${1}, name ${name}"
                if [ "x${1}" = "x${name}" ]; then
                    RESULT=$(( count + num ))
                    #echo "debug: match ${num}, result ${RESULT}"
                    echo "${RESULT}"
                fi
            fi
        done
}

setupnginxconfig() {
    local name=${1}
    local FQDN=${2}
    cat ~/git/deploy/usr/local/etc/nginx/sites-available/nginx-all \
        | sed "s/localhost.localdomain/${FQDN}/" \
        >$( ioc-pathof ${name} )/usr/local/etc/nginx/sites-enabled/${name}
    chmod 644 $( ioc-pathof ${name} )/usr/local/etc/nginx/sites-enabled/${name}
}

case ${1} in
    good)
        JAILNAMES="${JAILSGOOD}"
        ;;
    bad)
        JAILNAMES="${JAILSBAD}"
        ;;
    dev)
        JAILNAMES="${JAILSDEV}"
        ;;
    nginx)
        env IPNET="${IPNET}" HOSTIP="${HOSTIP}" HOSTNIC="${HOSTNIC}" setup_pfjails_ucarp
        setupnginxconfig nginx ${FQDN}
        exit
        ;;
    *)
        echo "ERROR: $0 [good|bad|dev|nginx]"
        exit 1
        ;;
esac

allowsysvipc() {
    iocage set allow_sysvipc=1 ${1}
}

copysslselfcert() {
    if [ "x" != "x${SQUIDIPALIAS}" ]; then
        if ifconfig | grep ${SQUIDIPALIAS} >/dev/null ; then
            cat $( ioc-pathof squid )/usr/local/etc/squid/public.pem >>$( ioc-pathof ${1} )/usr/local/share/certs/ca-root-nss.crt
        else
            curl --output $( ioc-pathof ${1} )/root/public-${SQUIDIPALIAS}.pem http://${SQUIDIPALIAS}:3127/public.pem
            cat           $( ioc-pathof ${1} )/root/public-${SQUIDIPALIAS}.pem >>$( ioc-pathof ${1} )/usr/local/share/certs/ca-root-nss.crt
        fi
    else
        cat $( ioc-pathof squid )/usr/local/etc/squid/public.pem >>$( ioc-pathof ${1} )/usr/local/share/certs/ca-root-nss.crt
    fi
}



env IPNET="${IPNET}" HOSTIP="${HOSTIP}" HOSTNIC="${HOSTNIC}" setup_pfjails_ucarp

echo "$JAILNAMES" \
    | while read num name ; do
    if [ "x" != "x${num}" ] && [ "x" != "x${name}" ]; then
        if ! iocage list | grep "${name}$" >/dev/null 2>/dev/null; then
            echo "Creating: ${num} ${name}"
            ioc-newjail common ${name} "${NIC}|${IPNET}${num}"
            # The following case can be in any order
            case ${name} in
                # GOOD JAILNAMES
                squid)
                    if [ -e /var/ports ]; then
                        rsync -qalmP --exclude work /var/ports/ $( ioc-pathof ${name} )/var/ports/
                    fi
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    iocage set boot=on ${name}
                    ;;
                easydnsclient)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                nginx)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    setupnginxconfig ${name} ${FQDN}
                    iocage set boot=on ${name}
                    ;;
                couch|couchdb)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=couchdb
                    iocage start ${name}
                    ;;
                dtfc)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_reposrc=${REPOSRC}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                uploadsh)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_reposrc=${REPOSRC}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                transfersh)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_reposrc=${REPOSRC}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                mdwiki)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_reposrc=${REPOSRC}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                piwik)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                owncloud)
                    if [         -d /root/local/owncloud ]; then
                        mkdir -p                          $( ioc-pathof ${name} )/root/local/owncloud
                        rsync -qalmP /root/local/owncloud/ $( ioc-pathof ${name} )/root/local/owncloud/
                    fi
                    allowsysvipc ${name}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    rsync -qalmP $( ioc-pathof ${name} )/root/local/owncloud/ /root/local/owncloud/
                    ;;
                ejabberd)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                murmur)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                vnc)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                klaus)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    iocage set boot=on ${name}
                    ;;
                gitlab)
                    if [ -e /var/ports ]; then
                        rsync -qalmP --exclude work /var/ports/ $( ioc-pathof ${name} )/var/ports/
                    fi
                    allowsysvipc ${name}
                    ## only if we are installing via src to cache rubygems
                    #copysslselfcert ${name}
                    if ifconfig | grep ${WEBIPALIAS} >/dev/null ; then
                        # Set jail ip as FQDN so git-shell works
                        grep -v " ${FQDN}" $( ioc-pathof ${name} )/etc/hosts >$( ioc-pathof ${name} )/etc/hosts.tmp
                        cat $( ioc-pathof ${name} )/etc/hosts.tmp >$( ioc-pathof ${name} )/etc/hosts
                        echo "${IPNET}`jailip gitlab` ${FQDN}" >>$( ioc-pathof ${name} )/etc/hosts
                        rm  $( ioc-pathof ${name} )/etc/hosts.tmp
                    fi
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}_config
                    iocage start ${name}
                    ;;
                builder|buildersquidpkg|builderffmpegpkg|buildgitlabpkg)
                    if [ -e /var/ports ]; then
                        rsync -qalmP --exclude work /var/ports/ $( ioc-pathof ${name} )/var/ports/
                    fi
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    rsync -qalmP --exclude work $( ioc-pathof ${name} )/var/ports/ /var/ports/
                    ;;
                # DEV JAILNAMES
                monit)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                zabbix)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                echoplexus)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_reposrc=${REPOSRC}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                gitannex)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                gogs)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}_src
                    iocage start ${name}
                    ;;
                phabricator)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                pydio)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                # BAD JAILNAMES
                baikal)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}
                    iocage start ${name}
                    ;;
                couchdtfc|couchdbdtfc)
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_reposrc=${REPOSRC}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=couchdbdtfc
                    iocage start ${name}
                    ;;
                horde)
                    allowsysvipc ${name}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_fqdn=${FQDN}
                    sysrc -f $( ioc-pathof ${name} )/etc/rc.conf.d/firstboot_jail firstboot_jail_deploy=${name}webmail
                    iocage start ${name}
                    ;;
            esac
            if [ "xsquid" != "x${name}" ]; then
                iocage stop ${name}
            fi
        fi # end of if not jail exists
    fi # end of if [ "x" != "x${num}" ] && [ "x" != "x${name}" ]; then
done
